Apache sub-configuration files are copied into this directory by the control
panel when the admin activates it.  However, this directory isn't maintained
by Git unless there is at least one file in it when it's checked in.
